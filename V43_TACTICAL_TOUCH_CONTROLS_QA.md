# v43 タクティカル・タッチコントロール意匠 QA

**対象:** STAR FOX — SKYBLAZER SQUADRON  
**更新日:** 2026-08-27  
**判定:** 意匠実装・縦横画面の操作回帰・視覚確認 合格

## 目的

v42で追加したモバイル向けのタッチ操作について、単に透明なボタンを並べるのではなく、戦闘中でも一目で役割を区別できる**航空機コックピットHUD**として再設計した。入力処理と操作位置の基本方針は維持し、形状、配色、階層、光沢、押下表現をCSSで強化している。3Dゲーム本体、戦闘ロジック、サウンド、カメラ、HUDのデータ更新処理は変更していない。

## 意匠方針

> **中心原則:** 透明度を保って飛行画面を隠さず、輪郭・色・形の三つで操作の役割を即時に判別できること。

左下の仮想スティックは、円形ではなく八角形の飛行制御パッドに変更した。外郭、内側の走査線、中心十字、可動ノブを重ね、操縦桿と照準器の中間にあるような輪郭を与えている。ノブはシアン系の高輝度アクセントを使用し、指の入力方向を見失いにくくした。

右下のアクション群は、薄い六角形のタクティカルパネル内に収めた。FIREは大きい琥珀色の八角トリガー、LOCKはシアンの照準キー、BOMBは橙赤の菱形キー、BOOSTはミント、BRAKEはマゼンタとして色を分けた。ロールは紫系の左右ペアレールに独立させ、PAUSEも青系の独立キーとしてパネル内に配置した。各キーは光沢のハイライト、内側の陰影、スキャンライン、発光輪郭を備える。

| 操作 | 形状・配置 | 主色 | 意図 |
|---|---|---|---|
| 仮想スティック | 左下の八角形パッド | シアン／ブルー | 飛行方向と照準器を想起させ、中心位置と入力方向を明確にする。 |
| FIRE | 右上寄りの大型八角トリガー | 琥珀／橙赤 | 主操作として最も大きい面積と暖色の視線誘導を与える。 |
| LOCK | FIRE左の小型キー | シアン | 照準・ターゲット操作としてスティックと同系統の色に統一する。 |
| BOMB | LOCK下の菱形キー | 橙赤 | 単発で重要なアクションを、形状と暖色でFIREから区別する。 |
| BOOST | 下段中央の角形キー | ミント | 加速を上向きの記号と冷色寄りの発光で表す。 |
| BRAKE | 下段右の角形キー | マゼンタ | 減速を下向きの記号と対照色で表す。 |
| ROLL | 最下段の左右レール | バイオレット | 左右という空間関係をそのまま形状へ反映する。 |
| PAUSE | 最下段右の独立八角キー | ブルー | 誤タップを避けるため、移動・攻撃キーから視覚的に分離する。 |

## 押下フィードバック

キーを押している間は、縮小、輝度・彩度の上昇、内側のハイライト、外側のグローを同時に適用する。FIREはさらに琥珀色の強い発光を行う。ゲーム側ではv42の仮想入力状態を継続利用しているため、ホールド操作は既存の連射、チャージ、ブースト、ブレーキにそのままつながり、単発操作はボム、ロール、一時停止に安全に接続される。

## レイアウト上の修正

初期デザインではロール、ブースト、ブレーキが同じグリッド段を使うため、縦型画面で重なる状態が確認された。このためアクション群を**4段の独立グリッド**へ再構成した。上2段はLOCK・BOMBと大型FIRE、3段目はBOOST・BRAKE、4段目は左右ROLLとPAUSEである。すべてのキーを独立セルへ収め、重なりを解消した。

## 検証結果

390×844の縦型と844×390の横型で、実際のタッチ対応ブラウザコンテキストを用いて確認した。各画面サイズでタイトルのタップ開始、仮想スティック、FIRE、LOCK、BOOST、BRAKE、BOMB、左ROLL、PAUSEを操作し、入力のオン／オフとゲーム状態を検証している。

| 検証項目 | 390×844 縦型 | 844×390 横型 |
|---|---:|---:|
| タッチキーの表示・パネル配置 | 合格 | 合格 |
| タイトル画面のタップ開始 | 合格 | 合格 |
| 仮想スティックの入力／解除 | `1 → 0` | `1 → 0` |
| FIRE、LOCK、BOOST、BRAKEのホールド／解除 | `true → false` | `true → false` |
| BOMBの発射 | 合格 | 合格 |
| 左ROLLの発動 | 合格 | 合格 |
| PAUSEの停止／再開 | 合格 | 合格 |
| キー同士の視覚的な重なり | なし | なし |
| JavaScript実行時エラー | なし | なし |

縦型の最終表示では、左下の八角形スティックと右下の4段パネルが分離し、FIRE、LOCK、BOMB、BOOST、BRAKE、左右ROLL、PAUSEがすべて独立して表示されることを確認した。横型の短い画面でも同じ配置原則を縮小適用し、HUDや戦闘画面を大きく覆わないことを確認した。

## 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `index.html` | 左右ROLLを専用ペアコンテナへ整理。スタイルシートのキャッシュバージョンを`mobile-v43`へ更新。 |
| `css/style.css` | v43の八角形スティック、タクティカルパネル、役割別カラー、グロー、スキャンライン、押下アニメーション、4段アクショングリッドを追加。 |

今回の変更はCSSと操作UIのマークアップに限定しており、v42で実装した入力連携を保持している。
