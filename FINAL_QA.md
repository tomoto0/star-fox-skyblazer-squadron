# STAR FOX — SKYBLAZER SQUADRON 最終QA記録

**記録日:** 2026-08-27  
**対象:** ローカル完成版（ES Modules + Three.js r166、ビルド工程なし）  
**結論:** 本QAの対象範囲において、起動、解放後導線、通常キャンペーンの再出撃、背景プロップ初期化、主要固定プール、およびブラウザ実行時の警告・例外を確認した。確認中に発見した近距離の大型背景プロップによる視界遮蔽を修正し、構文および実機表示で再検証した。

| 区分 | 判定 | 要約 |
|---|---|---|
| JavaScript構文 | 合格 | `src/**/*.js` の22モジュールを `.mjs` として `node --check` し、全件成功 |
| タイトル・解放表示 | 合格 | 保存済みクリアデータから Afterburner+ と Signal Archive 導線が復元された |
| Signal Archive | 合格 | `Q` で表示、`Q` でタイトルへ復帰する往復を確認済み |
| モード選択 | 合格 | `Enter` で選択画面、`1` で STANDARD REDEPLOY が開始された |
| STANDARD初期値 | 合格 | Wave 1 / Laser 1 / Bomb 3 / Lives 3 を実測 |
| 背景・視認性 | 改善済み | 近景プロップの遮蔽問題を修正し、通常Waveで中央の飛行回廊が明瞭であることを確認 |
| ブラウザ実行 | 合格 | 最新確認時に警告・例外は検出されなかった |
| 素材とクレジット | 合格 | 主要モデル・テクスチャ・パイロット肖像の存在を確認し、既存クレジットを保持 |

## 1. 今回の追加修正

最終プレイ確認において、初期スロットで生成された大型施設が、正式な飛行回廊の外側にありながらカメラ近傍へ到達した場合、画面端から中央視界を大きく占有し得ることを発見した。これは衝突や進行停止を起こす不具合ではないが、レールシューティングに必要な**敵弾・ロックオンUI・敵機の瞬時の認知**を損なうおそれがあるため、視認性上の改善対象と判断した。

`src/world/props.js` の `_rollProp()` における近景判定を、単純な目前範囲ではなく、スロットが再抽選されるまでに通過する近距離帯全体を含む `slot.z > -700` に拡張した。該当スロットの施設・岩塊は遠景レイヤーへ配置され、飛行回廊を飾るフレームとして残しつつ、プレイ空間を覆わないようになった。再読み込み後の実測では、近距離帯の主要プロップは `x = -753.8, 555.4, 625.0, 526.3, 647.9` など回廊外の遠景位置に置かれ、STANDARD開始画面でも中央の自機・照準・味方/敵マーカー周辺が確保された。

また、先行チェックで検出された `THREE.Material: flatShading is not a property of THREE.MeshToonMaterial` 警告は、`terrain.js` および `debris.js` で材質コンストラクタの引数から `flatShading` を除き、生成後にプロパティを設定する形へ是正済みである。今回の最新セッションでも、新規のThree.js警告やJavaScript例外は観測されなかった。

| 変更ファイル | 修正内容 | 品質上の効果 |
|---|---|---|
| `src/world/props.js` | カメラ近傍へ流入するプロップの遠景配置を強制 | 中央回廊、照準、敵弾の視認性を確保 |
| `src/world/terrain.js` | `MeshToonMaterial` の警告発生パターンを修正済み | 実行ログの健全性を改善 |
| `src/core/debris.js` | `MeshToonMaterial` の警告発生パターンを修正済み | 破片表現を保ったまま警告を解消 |

## 2. 静的診断・素材確認

最終状態の `src` 配下の全22 JavaScriptモジュールについて、通常のCommonJS判定による誤検知を避けるため、一時的に `.mjs` 拡張子へコピーしてから `node --check` を実行した。全モジュールが成功し、今回改修した `props.js` についても個別構文チェックを通過した。

主要素材については、共有金属ディテールテクスチャ、4名のパイロット肖像、外部背景施設、2種の英雄ランドマークを確認した。既存のCC0モデル・テクスチャおよびCC-BY BGMの利用方針は維持されており、`assets/CREDITS_ASSETPACK.txt` も削除・変更していない。

| 確認対象 | 結果 |
|---|---|
| `assets/textures/metal_diff.jpg` | 存在確認 |
| `assets/ui/portrait_bowie.png`、`portrait_vega.png`、`portrait_nova.png`、`portrait_kit.png` | 存在確認 |
| `assets/models/external/aero_station_ring.glb`、`scifi_antenna.glb` | 存在確認 |
| `assets/models/hero/hero_coast_rock.glb`、`hero_desert_rock.glb` | 存在確認 |
| `assets/CREDITS_ASSETPACK.txt` | 保持確認 |

## 3. ブラウザ総合確認

ローカル検証サーバーでタイトルから実際のキー操作を行い、保存済みのクリアデータ、Signal Archive、Afterburner+選択画面、STANDARDへの復帰を確認した。`localStorage` の `skyblazer-squadron-clear-data-v1` には、`afterburnerUnlocked: true`、`clears: 3`、`bestScore: 15750`、`bestAccuracy: 87` が保存されている状態を確認した。

STANDARD開始直後の内部アサーションは `state: playing`、`wave: 1`、`laser: 1`、`bombs: 3`、`lives: 3` を返した。加えて、背景プロップが初期化済み（`propsReady: true`）、循環地形チャンク数が8、固定大気ヘイズプール数が26であることを確認した。これは通常キャンペーンの標準ロードアウトと、重い生成を伴わない固定プール設計が起動時に適用されていることの確認でもある。

| 導線または機能 | 操作・検査 | 結果 |
|---|---|---|
| Signal Archiveを開く | タイトルで `Q` | CLEAR RECORDと周回用通信を表示 |
| Signal Archiveから戻る | Archiveで `Q` | タイトルへ復帰 |
| 解放状態の復元 | 再読み込み後に保存値を確認 | Afterburner+表示とArchive案内を再表示 |
| 出撃モード選択 | タイトルで `Enter` | STANDARD / AFTERBURNER+の選択画面を表示 |
| STANDARDへ戻る | 選択画面で `1` | Wave 1、標準HUD、僚機表示、残機3で出撃 |
| 循環地形・大気 | 起動後アサーション | terrain chunks = 8、haze = 26 |
| 実行ログ | 最新セッションのコンソール確認 | JavaScript例外・Three.js材質警告なし |

## 4. 既存検証の再確認対象

以下の記録により、今回の軽量な再確認に加えて、ゾーン固有表現、自然障害物の安全回廊、ランドマークの設置根拠、機体ディテールはすでに検証済みである。強制更新ループは検証ブラウザ固有のWebGLセッション喪失を誘発し得るため、今回は既に合格済みの破壊的な長時間テストを繰り返さず、導線・初期化・視界・警告の回帰に集中した。

| 既存記録 | 引き継ぐ確認根拠 |
|---|---|
| `WORLD_VISUAL_QA.md` | 全ゾーンの環境・遠景・画面可読性 |
| `LANDMARK_REALISM_QA.md` | 基礎・支持構造・外部ランドマークのスケールと位置 |
| `TERRAIN_NATURAL_HAZARDS_QA.md` | 自然障害物の安全回廊侵入0件とゾーン現象 |
| `SHIP_VISUAL_REFINEMENT_QA.md` | 自機・敵機・ボスの追加ディテールと損傷表現 |

## 5. 残存する検証環境上の留意点

Sandbox Browserでは、WebGLの連続的なシーン置換や強制的な大量 `update()` 呼び出しの後、ゲーム側の例外がなくても `about:blank` へ遷移することがある。この挙動は今回もゲームコードのエラーとしては再現せず、通常のタイトル、Archive、モード選択、STANDARD開始の操作では発生しなかった。したがって、本記録ではこれを**検証環境の制約**として扱い、ゲーム本体の既知不具合には分類していない。

ローカルの静的サーバーは検証用であり、恒久公開先ではない。将来の公開時は、アセット総量が大きいため、専用ホスティングでの実機ロード時間、モバイル操作、公開URLの継続性、および配布環境でのオーディオ自動再生ポリシーを別途確認することが望ましい。

## 最終判定

**合格（最終修正反映済み）。** 主要な起動・進行・解放・標準再出撃・描画初期化・ログ健全性を確認した。最終確認で発見された背景遮蔽を解消したため、現在の完成版は、爽快感を損なわない視認性、公平な飛行回廊、既存の高精細環境表現、解放後の周回導線を両立した状態である。
