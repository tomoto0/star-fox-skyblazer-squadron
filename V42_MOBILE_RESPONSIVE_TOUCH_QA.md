# v42 モバイル・レスポンシブUI／透明タッチ操作 QA

**対象:** STAR FOX — SKYBLAZER SQUADRON  
**更新日:** 2026-08-27  
**判定:** 実装・自動操作検証・縦横画面の視覚確認 合格

## 目的

本更新では、既存のデスクトップ向けキーボード／マウス操作とCanvasゲームの構造を維持したまま、スマートフォンを含む小型・可変画面に対応した。タッチ対応端末では、プレイ中だけ半透明の仮想スティックとアクションキーを表示する。縦型では戦闘中に必要な情報を優先し、僚機一覧、プレイヤー肖像、コンボ表示などの補助パネルを隠すことで、画面中心の戦闘空間と下部の操作領域を確保する。

## 実装対象

| ファイル | 更新内容 |
|---|---|
| `index.html` | 仮想スティック、FIRE、LOCK、BOMB、BOOST、BRAKE、左右ロール、PAUSEを持つモバイル操作レイヤーを追加。スタイルシートのキャッシュバージョンを`mobile-v42`へ更新。 |
| `css/style.css` | 小型横画面、縦型、低い横画面の各条件に合わせたHUD、メニュー、通信欄、タッチ操作の配置・文字サイズを追加。安全領域を考慮し、タッチ操作は半透明でゲーム画面を過度に遮らない。 |
| `src/core/input.js` | 既存のキー／マウス入力を保持し、仮想スティックを`moveX`／`moveY`へ、アクションキーを既存のキー状態へ統合。複数指、ポインタ解除、画面離脱、WebViewのポインタ捕捉失敗を安全に処理。 |
| `src/game/game.js` | タイトル画面のタップ開始、タッチによる一時停止／再開、プレイ中のみのコントロール表示を追加。ウェーブ結果、分岐、ゲームオーバー、勝利ではタッチ操作を隠す。 |

## モバイルの情報優先順位

> **常時表示:** シールド、ブースト、レーザー／ボム残量、スコア、残機、ボスHP、重要な通信、照準、ロックオン。

> **縦型で非表示:** プレイヤー肖像、僚機ステータス一覧、コンボカウンター、デスクトップ用のキーボード操作説明。

縦型のタイトル画面は「TAP TO START」と表示し、キーボード前提の操作説明を隠す。モード選択とルート分岐のキー表記も「TAP」へ置き換わる。ゲーム中の通信欄は下部コントロールの上に退避し、操作ボタンと重ならないよう調整した。

## タッチ操作仕様

| 操作 | UI | 既存ゲーム入力への接続 |
|---|---|---|
| 移動 | 左下の仮想スティック | `moveX`／`moveY`。既存の慣性、減速カーブ、速度依存感度をそのまま利用。 |
| 連射 | 右下の大きな`FIRE` | `Space`の押下状態として扱う。 |
| チャージ／ロックオン | `LOCK`をホールド | `KeyC`の押下状態として扱う。離すと既存のチャージ発射処理を使う。 |
| ボム | `BOMB` | `KeyB`の単発入力として扱う。空中ボムがある場合は既存どおり手動起爆する。 |
| ブースト／ブレーキ | `BOOST`／`BRAKE`をホールド | `ShiftLeft`／`ControlLeft`の押下状態として扱う。 |
| バレルロール | 左右のロールキー | `RollLeft`／`RollRight`の単発入力として扱う。 |
| 一時停止 | 右上の`Ⅱ` | 既存の一時停止状態を切り替える。 |

## 検証結果

390×844の縦型、844×390の横型について、タッチ対応モバイルコンテキストでゲームを読み込み、タイトル画面のタップ開始からプレイ状態までを確認した。両方の画面サイズでタイトルのキーボード操作説明は非表示となり、プレイ状態ではタッチ操作が表示された。

| 検証項目 | 390×844 縦型 | 844×390 横型 |
|---|---:|---:|
| タイトル画面のタップ開始 | 合格 | 合格 |
| タッチ操作レイヤーのプレイ中表示 | 合格 | 合格 |
| 仮想スティック右入力／解除 | `1 → 0` | `1 → 0` |
| FIREのホールド／解除 | `true → false` | `true → false` |
| LOCKのホールド／解除 | `true → false` | `true → false` |
| BOOSTのホールド／解除 | `true → false` | `true → false` |
| BRAKEのホールド／解除 | `true → false` | `true → false` |
| BOMBの発射 | 合格 | 合格 |
| 左ロールの発動 | 合格 | 合格 |
| PAUSEの停止／再開 | 合格 | 合格 |
| JavaScript実行時エラー | なし | なし |

縦型の視覚確認では、上部左右に必須ステータスを保持しつつ、僚機一覧・肖像・コンボ表示を除外できていることを確認した。下部には左の仮想スティックと右のアクション群を分離して配置し、通信欄はそれらの上へ置いた。横型の短い画面では、通信欄を左スティックより上へ移動し、スティック、アクション群、停止キーとの重なりを解消した。

## 運用上の注意

タッチ操作は、タッチ能力または粗いポインタが検出された端末で、出撃中にのみ表示される。デスクトップでのキーボード／マウス操作、既存のマウス照準、ゲームパッド非依存の操作系には影響しない。機種ごとにノッチやホームインジケータが異なるため、実公開後は代表的なiOS SafariおよびAndroid Chromeでも確認することが望ましい。

今回の対応はゲーム本体の戦闘ロジックを変えず、UIレイヤーと既存入力クラスへの入力統合に限定している。
