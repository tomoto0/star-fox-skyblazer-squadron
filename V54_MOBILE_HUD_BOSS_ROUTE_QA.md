# v54 モバイルHUD・TEMPEST RAY調整・モジュール配信ルート — QA記録

**対象:** STAR FOX — SKYBLAZER SQUADRON  
**更新日:** 2026-09-25  
**判定:** 合格

## 変更の概要

| 項目 | 変更内容 |
|---|---|
| TEMPEST RAY | Wave 3ボスの本体HPを **115 → 58** に半減。弱点ポッド・コアのHP、攻撃パターンは変更なし。 |
| 赤い警告テキスト（モバイル） | 縦長画面で`top`を **17% → 20%**、短い横長画面（高さ520px以下）で **22% → 25%** に変更。警告文、ゲームロジック、デスクトップ配置は維持。 |
| 僚機HPゲージ（モバイル） | 従来モバイルで非表示だった写真付きHPゲージを左上のHUD群に表示。縦長はHPバー直下の横並び、短い横長はHPバー右隣の横並び。 |
| モジュール配信 | 存在しない`.js`/`.mjs`、またはスクリプトとして要求された非JSパスは、ゲームHTMLではなく`text/javascript`の404を返す。正規エントリを`src/bootstrap.js`とした。 |

## モバイル僚機ゲージの配置

モバイルでは写真16px、名前、HPバーだけのコンパクトなチップにした。数値・LOCK表示の小さな文字は省き、状態は既存の枠色（LOCK=金、危険=橙の点滅、撃墜=半透明と灰色バー）で示す。

| 画面 | 配置 | 周辺HUDとの関係 |
|---|---|---|
| 縦長（幅700px以下） | HPバー群の直下（上端から61〜81px）に3枚横並び。幅は`clamp(54px, 18.5vw, 70px)`。 | ボスバーを56px → 85pxへ下げ、チップ・ボスバー・警告の順に重ならないよう配置。 |
| 短い横長（高さ520px以下） | HPバー右隣（x = 185px、上端8〜28px）に3枚横並び。 | ボスバー（上端48px）、スコア、操作ボタン、無線ウィンドウの領域外。 |

## 検証

Playwright（Chromium）でモバイル／デスクトップの各ビューポートを描画し、ボス出現中・僚機ゲージ表示中に全ボス接近警告とフェーズ警告を1つずつ表示して、要素矩形と文字の描画範囲を比較した。

| 確認項目 | 結果 |
|---|---|
| 縦長 320×568 / 360×640 / 375×667 / 390×844 / 414×896 / 430×932 | 僚機チップ、HPバー、スコア、ボスバー、警告、操作ボタンの重なりなし。 |
| 横長 740×360 / 844×390 / 896×414 / 932×430 | 同上。 |
| 横長 667×375 | 重なりなし（2行になる`AFTERBURNER+`告知も、文字はボタン上端の6px上で止まる）。 |
| 横長 568×320 | 警告文字はボスバー下端の4px下から始まる。2行になる`AFTERBURNER+`告知だけは、変更前と同じく右下の操作パネルに一部かかる。 |
| デスクトップ 1440×900 / 800×600 | 警告・僚機ゲージ・ボスバーの位置が変更前と同一。 |
| TEMPEST RAY | 出現時`hp = maxHp = 58`。両ポッド破壊後にHP 6でコア露出、コアへの3射で撃破。 |
| 配信ルート | `/src/bootstrap.js` → 200 `text/javascript`、`/bootstrap.js`・`/src/src/bootstrap.js`・`/play/src/bootstrap.js` → 404 `text/javascript`（`no-store`、`nosniff`）。`/`・OGP・画像・CSSの応答は従来どおり。 |
| 起動 | `bootstrap.js` → `main.js?v=mobile-hud-v54`の順で読込、コンソールエラーなし。モジュール欠落時は画面に読込失敗の通知を表示。 |

## 変更ファイル

| ファイル | 内容 |
|---|---|
| `src/entities/bosses.js` | TEMPEST RAYの本体HPを58に変更。 |
| `css/style.css` | モバイルの警告位置、僚機チップの表示と配置、縦長画面のボスバー位置。 |
| `server.mjs` | スクリプト要求に対するJavaScript型404。ルートのHTMLはスクリプト要求には返さない。 |
| `src/bootstrap.js` | 正規モジュールエントリ。`?v=`キーを`main.js`へ引き継ぎ、読込失敗を画面に通知する。 |
| `index.html` | エントリを`src/bootstrap.js`へ変更し、CSS・JSのキャッシュキーを`mobile-hud-v54`へ更新。 |
| `README.md`、`CLAUDE.md` | エントリ構成と配信ルールを追記。 |
