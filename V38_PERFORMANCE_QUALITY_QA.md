# v38 描画・環境負荷 最適化 QA

**対象:** STAR FOX — SKYBLAZER SQUADRON  
**更新日:** 2026-08-27  
**判定:** 合格

本更新は、戦闘・操作・被弾判定を変更せず、描画解像度、Bloom、遠方景観、背景アニメーションを品質段階に応じて制御することで、重い場面の負荷を下げることを目的とする。すべての敵AI、弾道、衝突、入力更新、HUD更新は従来どおりフレームごとに実行される。

## 監査結果と最適化方針

実機の即時メトリクスでは、最適化前の負荷が高い戦闘状態において、シーン内に6,874オブジェクト、4,808メッシュ、1,095スプライトが存在した。環境側には8個の地形チャンク、20個の自然現象、126個の発光装飾があり、フルスクリーンのBloom合成も常時実行されていた。これを踏まえ、ゲームプレイに影響しない表示・装飾の経路を優先的に調整した。

| 優先対象 | 実装した軽量化 | 保持した要素 |
|---|---|---|
| フルスクリーン後処理 | 三段階の品質制御を追加。高負荷時はBloom内部解像度を下げ、緊急段階では合成チェーンを停止して直接描画へ退避 | 色空間、トーンマッピング、ワールド描画、HUD、戦闘演出のロジック |
| レンダラー解像度 | `BALANCED` 1.25、`PERFORMANCE` 1.00、`EMERGENCY` 0.85を上限とするピクセル比を導入 | アスペクト比、カメラ、操作座標、UIレイアウト |
| 地形チャンク | フォグ端に300ユニットの重複を残したまま、遠方チャンクを非表示化 | 近景地形、障害物、航路、チャンク移動・再利用、衝突判定 |
| 背景プロップ | 背景プロップ16、背景艦5、ヒーローランドマーク2を品質別描画距離でカリング | 近景シルエット、航路脇のランドマーク、再出現・再配置ロジック |
| 環境アニメーション | 浮島、水煙、霞、モート、リフト、発光装飾の変形更新を30/22/15 Hzへ間引き | 地形・障害物・プレイヤー・弾・衝突・水面のスクロールとゲーム進行はフルレート |

## 品質制御

品質コントローラは、フレーム時間の指数移動平均を用いる。34msを超える状態が1.2秒続くと一段階軽量化し、22ms未満が5.5秒続くと一段階復帰する。各切替後にはクールダウンを設け、境界付近で品質が頻繁に上下する現象を避ける。

| 段階 | ピクセル比上限 | Bloom | 背景更新 | 描画距離 | 用途 |
|---|---:|---|---:|---:|---|
| `BALANCED` | 1.25 | 有効、内部係数0.62 | 30Hz | 1,500 | 通常の十分な描画性能 |
| `PERFORMANCE` | 1.00 | 有効、内部係数0.48 | 22Hz | 1,150 | やや重い戦闘・高解像度画面 |
| `EMERGENCY` | 0.85 | 無効、直接描画 | 15Hz | 820 | 持続的な負荷時。近景と戦闘情報を最優先 |

> 品質段階の変更対象は表示コストだけである。敵の数、弾数、弾速、攻撃タイマー、プレイヤーの加速・減速・照準、衝突判定、当たり判定、スコア進行は変更しない。

## 実機検証

| テスト | 結果 |
|---|---|
| 起動修正 | `Atmosphere.setAmbientRate()`の依存関係を修正後、クリーンセッションで`window.__game`の初期化を確認 |
| `PERFORMANCE`初期化 | Sandbox実機では高いフレーム時間を検知し、ピクセル比1.00、Bloom有効、地形・大気22Hzで自動選択 |
| `EMERGENCY`直接描画 | ピクセル比0.85、Bloom無効、地形・大気15Hzを確認。直接描画で654 draw calls、32,812 triangles、246 pointsを取得 |
| 遠方チャンク | `BALANCED`で7/8、`EMERGENCY`で5/8チャンクを可視とすることを確認。フォグ端の重複を保持 |
| 実戦カリング | `EMERGENCY`中、地形4/8、プロップ7/16、背景艦2/5、ヒーローランドマーク1/2を可視として近景を保持 |
| 実画面回帰 | 自機、僚機、HUD、海面、雲、航路ビーコン、近景の岩・遺跡・背景シルエットを同時に確認。中央飛行レーンの視認性を維持 |
| 固定プール | 実戦サンプルでポイント粒子0/900、敵弾2、デブリ0。固定プールの上限・再利用方針に変更なし |
| 構文 | `src`配下の全22 JavaScript ES Moduleで構文検証に成功 |
| 実行ログ | 最終の品質制御・カリング・環境更新間引きに起因するJavaScript例外または描画警告なし |

## Sandbox検証上の注記

120フレーム連続のブラウザ内サンプリング、および長い制御ループは、Sandbox Browserの既知のWebGLセッション不安定性によってタイムアウトまたは`about:blank`遷移を引き起こした。このため、実機検証は新規セッション、即時レンダラー統計、短時間の品質段階アサーション、実際の出撃画面で行った。失われたセッションの前後でソース構文は正常であり、修正後のクリーンセッションは問題なく初期化・出撃・描画された。

## 結論

ゲームは、十分な性能がある環境ではBloomと遠景を保つ`BALANCED`表示を用い、負荷が上がると自動的に`PERFORMANCE`、必要時には`EMERGENCY`へ移行する。負荷軽減は主にピクセル数、フルスクリーン合成、フォグ外の地形・背景、装飾アニメーションに限定した。したがって、ゲームプレイの公平性、操作感、敵・障害物の読みやすさ、近景の景観品質を損なわずに、重い環境での実用的な負荷低減を実現している。
