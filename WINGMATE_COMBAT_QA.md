# 僚機戦闘AI・耐久性 QA記録

**対象:** `STAR FOX — SKYBLAZER SQUADRON`  
**記録日:** 2026-08-27  
**結論:** HP偏りの原因は、僚機が実弾を個別に受けた結果ではなく、救援イベントの警告タイマーが毎秒15 HPを直接減らす旧仕様、およびプレイヤー狙いの流れ弾を全僚機が受け得る二次的な衝突判定にあった。敵は通常時にはプレイヤーのみを照準しており、僚機を意図的に狙うシステムは未実装だった。現在は、僚機への明示的なターゲット選択、重複ロック上限、対象指定弾だけの被弾、短時間の被弾クールダウンを実装し、制御テストとブラウザ実機で確認した。

| 確認項目 | 旧仕様 | 修正後 | 判定 |
|---|---|---|---|
| 通常敵の照準 | プレイヤー固定 | プレイヤー主体のまま、対応敵の限定割合が僚機を一時ロック | 改善 |
| 救援イベント | `danger` 中に毎秒15 HPを自動減算 | 実際の追跡敵が僚機を照準。放置時も固定14 HPの退避ペナルティのみ | 改善 |
| 流れ弾の僚機被弾 | プレイヤー狙いの弾も近い僚機へ命中 | 明示的に僚機を狙った弾だけが、その僚機へ命中 | 改善 |
| 集中被弾 | 複数敵が同一僚機へ重なる可能性 | 通常敵は各僚機1機まで。救援追跡機のみ例外 | 改善 |
| 連続ダメージ | 同フレームの複数弾が重なる可能性 | 0.36秒の被弾クールダウン | 改善 |
| 出撃時HP | `reset()` がHPを明示初期化しない | 全僚機をHP 100へ明示初期化 | 改善 |
| ターゲット可視化 | 危険イベントのみ警告 | HUDに `LOCK` / `LOCK×n`、世界空間に琥珀色のロック表示 | 改善 |

## 1. 原因調査

旧実装では、敵AIの照準関数がプレイヤーの座標だけを参照していた。その一方で、敵弾の衝突処理は、プレイヤー判定の後に全僚機との距離を走査していた。このため、僚機が受ける通常被害は、敵がその僚機を狙った結果ではなく、プレイヤー狙いの弾が偶然その飛行軌跡と交差した結果だった。

さらに、救援イベントではランダムに選ばれた僚機の `danger` を7.5秒に設定し、イベント中にHPを毎秒15ずつ減らしていた。救援に失敗すると約112.5 HP相当が失われるため、実際の敵弾命中を確認できないまま特定の僚機だけが急速に離脱するように見えた。実機観測でも、NOVAが `HP 31.3 / DOWN` の一方、VEGAとKITがHP 100となる偏りが確認できた。

> 旧挙動は「敵が僚機を狙っているように見える」演出と、実際の被弾処理が分離していた。この分離を解消し、ターゲット、飛翔弾、衝突、HUDを同じ対象参照で結ぶことを改修方針とした。

## 2. 実装した改善

敵AIには、短時間の `allyTarget` を追加した。通常戦闘ではプレイヤーを主目標として維持し、僚機狙いは対応敵種の**16%**に限定している。僚機候補は敵の攻撃距離内にいる必要があり、候補間では現在のロック数が少ない機体を優先する。通常敵は同じ僚機へ同時に重複ロックできず、各僚機の通常ロックは最大1機となる。

救援イベントで生成されるランサーには、その救援を要求した僚機を `forcedWingmate` として明示的に設定した。これにより、警告、敵機の機首、発射する弾、僚機の被弾はすべて同じ対象を参照する。救援を放置した場合も、自動的な大幅HPドレインではなく、14 HPの固定退避ペナルティと短い修理離脱へ変更した。

被弾は、`targetWingmate` を持つ敵弾だけが、その指定僚機に対して判定される。プレイヤーへ向けた通常弾が、偶然近くを通った僚機のHPを不規則に削ることはない。僚機側の `takeDamage()` は1回の有効命中後に0.36秒のクールダウンを適用し、弾幕が同フレームに集中しても一瞬で耐久を失わないようにした。

| 実装箇所 | 主な変更 |
|---|---|
| `src/entities/wingmates.js` | イベント連動HPドレインの撤去、`takeDamage()`、0.36秒クールダウン、HP再初期化、救援追跡機の明示ターゲット化 |
| `src/entities/enemies.js` | `allyTarget`、敵種別の僚機照準可否、16%の限定ロック、通常時1僚機1ロック、機首の照準同期 |
| `src/entities/projectiles.js` | 敵弾へ`targetWingmate`を追加し、ホーミング弾も同じ僚機を追尾 |
| `src/game/game.js` | 対象指定弾だけが対象僚機に命中する衝突判定へ変更 |
| `src/ui/hud.js`、`css/style.css` | `LOCK` / `LOCK×n` の表示と琥珀色のロック状態を追加 |

## 3. 検証結果

ブラウザ上でWave 1を開始し、HP初期化、明示ターゲット弾、非指定弾、重複ロック制御を確認した。いずれも強制更新ループを使わず、通常の描画・更新中、または単発の衝突判定によって検証している。

| テスト | 操作 | 観測結果 | 判定 |
|---|---|---|---|
| 出撃時のHP初期化 | STANDARD開始 | VEGA / NOVA / KIT がすべてHP 100 | 合格 |
| 明示ターゲット弾 | VEGAを狙うランサーを1機配置 | VEGAのみHP 100→93、NOVA/KITは100を維持。HUDに`LOCK`を表示 | 合格 |
| 非指定の流れ弾 | VEGA位置へプレイヤー狙いの重弾を1発置く | VEGA HPは100のまま。僚機への誤ダメージなし | 合格 |
| 集中弾のクールダウン | VEGAへ重弾3発を同時到達させる | HP 100→88の1回分のみ。クールダウン0.36秒を確認 | 合格 |
| 重複ロック上限 | VEGAを初期指定したランサー3機を単発更新 | 敵ターゲットは`[null, null, VEGA]`、VEGA `targetedBy: 1`、他僚機0 | 合格 |
| HUD表示 | 上記のロック状態を表示 | ロック対象だけが`LOCK`、対象外は数値HP | 合格 |
| 構文 | 全`src/**/*.js`をES Moduleとして検査 | 22モジュールすべて成功 | 合格 |
| 実行ログ | 最新ブラウザセッションを確認 | 新規JavaScript例外・Three.js警告なし | 合格 |

## 4. 仕様上の結果

敵は現在、**僚機もきちんと狙う**。ただし、プレイヤーを主目標として保つため、全敵が常時僚機へ集中する設計にはしていない。通常は一部の対応敵が短時間だけ僚機をロックし、各僚機には通常敵1機までという上限を設けた。救援発生時だけは、当該僚機の背後に実体のある追跡機が付き、プレイヤーが撃破すれば危険を解消できる。

この変更により、各僚機の耐久差は個別の実被弾・救援成功失敗・修理離脱に基づく説明可能な差になる。理由なく1機だけHPが急落するケースと、敵が明らかに狙っていないのに別機が被弾するケースは除去されている。

## 最終判定

**合格。** 僚機のHP・被弾・敵照準・HUD表示を一貫した対象参照で接続し、不透明なHP偏りを解消した。現在は、敵の僚機狙いを見て判断し、ロック表示を手掛かりに救援へ介入できる公平な支援AI挙動になっている。
