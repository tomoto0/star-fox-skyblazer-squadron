# V48 スコアランキング機能 QA

**対象:** STAR FOX — SKYBLAZER SQUADRON  
**改修版:** `rankings-v48`  
**確認日:** 2026-08-27  
**判定:** **合格（機能・画面・終了フロー・モバイル回帰）**

## 1. 実装目的

本改修では、プレイ終了時のスコアを**上位30位**まで保持し、入賞したプレイヤーだけが好きなコールサインを登録できるランキング機能を実装した。ランキングはタイトル画面からいつでも閲覧でき、ゲームオーバー時・キャンペーン完走時のどちらでも入賞判定を行う。現行のローカル実行環境では端末内保存を使用し、将来のManus Platform上でのクラウドランキングへ移行できるよう、保存処理を非同期リポジトリ境界として分離している。

> 現バージョンのランキングは **LOCAL FLIGHT RECORDS** と明記している。ほかのプレイヤーや他端末と共有されるオンラインランキングであると誤認させない表示である。

## 2. プレイヤー体験

| 場面 | 操作・表示 | 挙動 |
|---|---|---|
| タイトル画面 | `PILOT RANKINGS` ボタンまたは PC の `R` キー | 現在端末に保存された上位30件を閲覧する。モバイルではキーヒントを非表示にし、ボタン操作のみで完結する。 |
| ランキング画面 | スコア順の表 | 順位、コールサイン、スコア、ラン種別を表示する。1～3位は金・銀・銅系の色で区別し、新規保存直後の行は金色のアクセントで強調する。 |
| ゲームオーバー | 650ms後に入賞判定 | 最終スコアがランクインする場合のみ、コールサイン登録画面を表示する。 |
| キャンペーン完走 | エンディング表示後 4.45秒で入賞判定 | 最終デブリーフ演出を優先し、演出完了後に登録画面を表示する。 |
| コールサイン登録 | 1～14文字、`REGISTER SCORE` | 入力をサニタイズして保存し、保存後は該当行を強調したランキング表へ遷移する。 |
| 見送り | `NOT NOW` または `Esc` | スコアを保存せず、タイトル画面へ戻る。 |

## 3. 上位30位の判定仕様

ランキングは降順で並び、最大30件だけを保存する。すでに30件ある場合は、既存の30位より**厳密に高い**スコアだけを入賞として扱う。同点は現在の30位を押し出さないため、同スコアでランキングが不安定に入れ替わることはない。30件未満の場合は、1点以上のスコアであれば登録対象となる。

| 条件 | 名前入力 | 保存 | 表示 |
|---|---:|---:|---|
| 上位30位内（30件未満を含む） | 必須 | 可能 | 順位を予告して登録画面を表示 |
| 30件満了・30位より高得点 | 必須 | 可能 | 新しい順位で登録画面を表示、31位を切り捨て |
| 30件満了・30位と同点以下 | 不可 | しない | 登録画面を出さず従来の終了画面へ戻る |
| 空欄・制御文字のみ | エラー表示 | しない | 入力欄を維持して再入力を促す |
| 端末ストレージが使用不可 | エラー表示 | しない | ゲーム終了・再挑戦には影響させない |

## 4. 保存設計と将来のクラウド移行

`src/core/leaderboard.js` に、ランキングの表示・判定側から保存先を分離する `LeaderboardStore` を実装した。現行アダプタは `LocalLeaderboardRepository` であり、`localStorage` のキー `skyblazer-squadron-leaderboard-v1` に端末内記録を保持する。ゲーム終了処理とUIは `read()`、`write()`、`submit()`、`refresh()` という非同期インターフェースだけを利用するため、保存先を差し替えても画面・順位判定・ゲームフローを変更する必要がない。

| 層 | 現行実装 | Manus Platformへの移行時 |
|---|---|---|
| UI・ゲームフロー | `src/game/game.js` | 変更不要。非同期ストアAPIを継続利用する。 |
| 順位・入力正規化 | `LeaderboardStore` | クライアントの即時表示補助として維持する。最終判定はサーバー側を正とする。 |
| 保存アダプタ | `LocalLeaderboardRepository` | `ManusLeaderboardRepository` へ差し替え、API経由でデータベースへ保存する。 |
| 永続データ | この端末の `localStorage` | Manus Databaseのランキングテーブルへ保存し、全プレイヤーの上位30位を共有する。 |

クラウド化では、クライアントが「上位30件の配列」全体を書き換える方式を採用しない。代わりにサーバーが1回のプレイ記録を検証して受け取り、トランザクション内で順位を確定する。推奨するデータ項目は、`id`、`player_name`、`score`、`mode`、`outcome`、`wave`、`kills`、`accuracy`、`created_at` である。公開ランキングの公平性を保つため、将来的にはサーバー側でセッション識別、スコアの上限・整合性検証、レート制限、投稿元の監査を追加する。

## 5. UI設計

ランキング画面は、既存のタイトル背景とHUDに合わせて、濃紺のテレメトリーパネル、シアンのエッジ、金色のスコア強調、走査線を用いた。全30行はパネル全体ではなく中央の表領域だけをスクロールさせるため、戻る操作は常に画面下部に残る。入賞登録画面は、検知通知、最終スコア、予測順位、入力欄、登録／見送りの順に情報を並べ、入力操作を迷わせない。

縦型モバイル（390×844）では、ランキングパネルは画面内に収まり、表領域だけがスクロールする。PC向けの `PRESS R` と `ESC` ヒントは隠し、タッチで戻るボタンを操作する構成とした。

## 6. テスト結果

| 検証 | 手順 | 結果 |
|---|---|---|
| タイトル導線 | タイトル画面からランキングボタンを選択 | 空のランキング画面を表示。タイトル背景を維持。合格。 |
| 入賞登録 | 456,789点を1位として投入し、`Bowie <Ace>` を登録 | `#1`、`456,789`、新規記録ハイライトを確認。文字列は安全にテキスト表示。合格。 |
| 上位30件上限 | 30件を降順で保存後、30位未満と30位超えを判定 | 30件維持、15,000点は不採用、31,000点は採用。合格。 |
| ゲームオーバー経路 | 実際の `_loseLife()` 終了処理から確認 | 15,750点で登録画面を表示。合格。 |
| キャンペーン完走経路 | 実際の `_victory()` 終了処理から確認 | エンディング猶予後、265,000点で登録画面を表示。合格。 |
| 構文・実行時例外 | ESM構文検査およびChromium実画面テスト | JavaScript例外なし、ゲーム関連HTTP失敗なし。合格。 |
| モバイル画面 | 390×844、30行のランキングを表示 | パネル位置 `top: 34px` / `bottom: 810px`、表領域 `539px` をスクロール可能として確認。合格。 |

## 7. 変更ファイル

| ファイル | 変更内容 |
|---|---|
| `src/core/leaderboard.js` | 上位30件の整列・上限・コールサイン正規化・端末保存・将来用非同期リポジトリ境界を追加。 |
| `src/game/game.js` | タイトルからの閲覧、ゲームオーバー／完走時の入賞判定、登録・見送り・タイトル復帰の遷移を追加。 |
| `index.html` | ランキング画面、コールサイン登録モーダル、タイトルボタンを追加。CSS・JSキャッシュ識別子を `rankings-v48` へ更新。 |
| `css/style.css` | PC・モバイル対応のテレメトリー風ランキング表、入賞登録、フォーカス・新規行強調スタイルを追加。 |

## 8. 結論

V48により、プレイ終了時の上位30位入賞登録とタイトル画面からのランキング閲覧を実装した。現行では端末内保存で安定動作し、オンライン性を誤認させないUIを備える。保存境界は将来のManus Database連携を前提に分離済みであり、クラウド化ではサーバー側の検証・トランザクション処理を追加して共有ランキングへ安全に移行できる。

## 9. 関連記録

| 種別 | パス |
|---|---|
| 自動機能テスト | `/tmp/starfox-mobile-qa/leaderboard_v48_results.json` |
| 終了フロー回帰 | `/tmp/starfox-mobile-qa/leaderboard_v48_endflow_results.json` |
| モバイル回帰 | `/tmp/starfox-mobile-qa/leaderboard_v48_mobile_results.json` |
| 視覚確認メモ | `/tmp/starfox-mobile-qa/leaderboard_v48_visual_findings.txt` |
| デスクトップランキング画像 | `/tmp/starfox-mobile-qa/v48-leaderboard-saved.png` |
| コールサイン登録画像 | `/tmp/starfox-mobile-qa/v48-rank-entry.png` |
| モバイルランキング画像 | `/tmp/starfox-mobile-qa/v48-leaderboard-mobile.png` |
